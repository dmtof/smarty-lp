/* let pointItemDot = document.querySelectorAll('.point-item');

for (let i = 0; i < pointItemDot.length; i++) {
    pointItemDot[i].addEventListener('click', function(num) {
        document.getElementById(`pointItem${i}`).classList.toggle('choosen-point')}, false);
} */